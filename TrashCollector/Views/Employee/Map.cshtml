
@model TrashCollector.Models.Customer

@{
    ViewBag.Title = "Map";
}

<h2>How do I get there?</h2>

<style>
    /* Always set the map height explicitly to define the size of the div
        * element that contains the map. */
    #map {
        height: 400px;
        width: 700px;
    }
</style>
<body>
    <center><h3>Milwaukee WI</h3></center>
    <br />
    <div class="row">
        <center><div id="map"></div></center>
        <script>
            var map;
            //var geocoder = new Geocoder(AIzaSyDQnNTtPkc5Zztdqlg18DT - 3wjGF1w0t2k);
            //function GetLatAndLong() {
            //    var customer = db.Customers.Where(n => n.ID == id).FirstOrDefault();
            //    var normalAddress = customer.Address;
            //    var mapAddress = geocoder.Geocode(normalAddress);
            //}
            function initMap() {
                var pickup = { lat: 43.038902, lng: -87.906471 };
                //var pickup = { "https://www.google.com/maps/embed/v1/place?key=AIzaSyAuzzUVbumsRjeMkAXRleluKFBXkWeTmzo&q="Street.Replace(' ', '+') + "+" + Model.City + "+" + Model.State + "+" + Model.Zip };
                var map = new google.maps.Map(document.getElementById('map'), {
                    center: pickup,
                    zoom: 13
                });
                var marker = new google.maps.Marker({ position: pickup, map: map });
            }
        </script>
        <script src="https://maps.googleapis.com/maps/api/js?key=@ViewBag.Keys&callback=initMap";
          

                async defer></script>
    </div>
    @*@{
            string address = "https://www.google.com/maps/embed/v1/place?key=AIzaSyAuzzUVbumsRjeMkAXRleluKFBXkWeTmzo&q=" + Model.Street.Replace(' ', '+') + "+" + Model.City + "+" + Model.State + "+" + Model.Zip;

            <iframe width="600"
                    height="450"
                    Zoom="12"
                    frameborder="0" style="border:0"
                    src="@address" allowfullscreen></iframe>
        }*@
</body>

